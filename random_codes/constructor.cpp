#include <iostream>
using namespace std;
// one class can have multiple constructors which is known as constructor overloading. First write default constructor and then overload it.
// constructors are always inside the class
class Room{
    public:
    int length,breadth,height;

    Room(){
        cout << "From constructor method";
    }

    int area(){

    }
};
int main(){

return 0;
}